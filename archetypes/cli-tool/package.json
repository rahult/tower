{
  "name": "cli",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "bin": { "cli": "./bin/cli.js" },
  "scripts": {
    "test": "vitest run",
    "verify": "npm run -s test",
    "accept": "node scripts/acceptance.mjs"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
