{
  "name": "api",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "node src/index.js",
    "test": "vitest run",
    "verify": "npm run -s test",
    "accept": "node scripts/acceptance.mjs"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
