packages:
  - "packages/*"
  - "spikes"

# esbuild is needed by Vite/Vitest; the other two are pi transitive deps we never execute.
allowBuilds:
  esbuild: true
  "@google/genai": false
  protobufjs: false
